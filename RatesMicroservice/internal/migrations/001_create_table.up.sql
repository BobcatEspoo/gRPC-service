CREATE TABLE answers (
    id SERIAL PRIMARY KEY,
    request VARCHAR(32),
    time VARCHAR(32),
    price VARCHAR(32),
    volume VARCHAR(32),
    amount VARCHAR(32),
    factor VARCHAR(32),
    type VARCHAR(32)
);
